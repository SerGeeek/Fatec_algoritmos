# 8) Considerando 10 números reais digitados pelo usuário, exibir o menor deles.

cont = 1
menor = float(input("Valor: "))
while cont <= 9:
    num = float(input("Valor: "))
    if num < menor:
        menor = num
    cont = cont + 1
print ("O menor número é: ", menor)
